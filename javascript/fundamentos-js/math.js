// Usando o objeto Math

/* Em JS nos usamos o objeto Math para facilitar os nossos cálculos
como exemplo vamos descobrir um diâmetro de uma área */

const raio = 5.6
const area = Math.PI * Math.pow(raio,2)

console.log(area);

// a função typeof é para descobrir qual é o tipo do valor informado
console.log(typeof Math);