// Exemplos Básicos sobre Funções 

function imprimirSoma(n1, n2){ //função sem retorno
    console.log(n1 + n2);
}
imprimirSoma(2, 2)

function soma(numero1, numero2) { // função com retorno (return)
    return (numero1 + numero2)
}
console.log(soma(4, 4));

function brunotop(b){
    console.log("Bruno é top");
}
brunotop()

/* é bem simples declaramos a função com a palavra reservada (function) e em seguida passamos os
devidos parâmetros. */

//----------------@brunomarcosluz----------------------//